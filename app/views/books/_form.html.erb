<%= form_for(@book) do |f| %>
  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'Link to Library catalogue or other source' %><br>
    <%= f.text_field :link %>
  </div>
  <div class="field">
    <%= f.label :ISBN %><br>
    <%= f.text_area :isbn %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
